{
  "name": "beard-trim-map-cloudflare-rootonly-v11-1",
  "private": true,
  "version": "11.1.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "wrangler deploy --dry-run"
  },
  "devDependencies": {
    "wrangler": "latest"
  }
}